node_modules/
dist/
coverage/
*.local
*.local.json

# Secrets. .env.example is committed; .env never is.
.env
.env.*
!.env.example

# Local working notes and generated artifacts
DECISIONS-PENDING.md
dependency-graph.svg
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/

# Model-call logs — real sessions become eval fixtures, but they contain
# whatever business a person narrated. They stay local.
logs/

.DS_Store
*.log
.vite/
.eslintcache

# playwright-cli session artifacts (snapshots, console logs, traces)
.playwright-cli/

# @playwright/test run output (e2e/ itself is committed — spec + fixtures)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# impeccable design skill. Vendored copy lives at .claude/skills/impeccable/;
# the per-harness mirrors and per-user state are local only. The committed
# reference comps under .impeccable/mocks/ are force-added; everything else here
# is local (regenerate with the skill).
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/questions/
.impeccable/mocks/
.github/skills/
.github/agents/
.github/hooks/
